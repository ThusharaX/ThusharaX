name: GitHub Readme YouTube Cards
on:
  schedule:
    # Runs every hour, on the hour
    - cron: '0 0 * * 3'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: DenverCoder1/github-readme-youtube-cards@main
        with:
          channel_id: UC1nRtpAly0QnFHUcFZfDwow
          comment_tag_name: YOUTUBE-CARDS
          show_duration: true
